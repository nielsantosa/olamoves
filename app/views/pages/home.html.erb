<div class="banner">
  <div class="banner-items">
    <h1>DISCOVER WELLNESS THROUGH MOVEMENT</h1>
    <div class="search-container">
      <form action="/action_page.php">
        <input type="text" placeholder="Search.." name="search">
        <button type="submit"><i class="fa fa-search"></i></button>
      </form>
    </div>
  </div>
</div>
  <div>
    <video controls width="400" autoplay src="<%= @video_url %>" type="<%= @video_type %>"> <!-- Showing the video type -->
      Sorry, your browser doesn't support embedded videos. <!-- Alternate text if the video cannot be not shown -->
    </video>
  </div>


  <p class>Popular Classes</p>
  <% @videos.each do |video| %>
      <video controls width="400" autoplay src="<%= video.video_url %>" type="video/ogg"></video>
    <%= video.title %>
    <%= video.price %>
  <% end %>
</div>
<%=  %>

</div>

<div>
  <p class>Latest Classes</p>
</div>
